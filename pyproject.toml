[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agnitra"
version = "0.1.0"
description = "Agnitra project"
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
agnitra = "cli.main:main"
agnitra-optimize = "cli.optimize:main"
