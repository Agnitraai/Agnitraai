# Llama-3-8B / H100 Benchmark — Results

_This file is generated by `compare.py` from JSON outputs in `raw/`._

**No published numbers yet.** Run `./run.sh` on an H100 to populate this
file. Until that happens, treat any performance claim about Agnitra as
unverified.

To reproduce on an H100:

```bash
HF_TOKEN=hf_xxx ./run.sh
```

Then commit the regenerated `RESULTS.md` and the contents of `raw/` so
reviewers can verify the math.

See `README.md` in this directory for the full methodology.
